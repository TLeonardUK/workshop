# ================================================================================================
#  workshop
#  Copyright (C) 2022 Tim Leonard
# ================================================================================================
# This file just contains commonly used param blocks which are generic to specific shaders.
# ================================================================================================
type: Shader
version: 1

param_blocks:

  # State of the camera being used to render the scene.
  camera_state:
    scope: global
    fields:
      mvp_matrix: matrix4
      near_clip: float
      far_clip: float